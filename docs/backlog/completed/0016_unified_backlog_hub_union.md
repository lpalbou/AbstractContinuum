# Completed: unified backlog — Board/Backlog read the hub union (work: rows)

> Priority: P0
> Labels: operator-intake, board, unification
> Type: feature

- Work id: `abstractcontinuum-0016`
- Thread anchor: operator dm 105 ("we MUST have a unified backlog system
  across agents... NOTHING to do with gateway — those backlog items should
  exist beyond gateway") → contract proposal commons c3328 (agora + skill
  asked: work:<id> store rows as the cross-agent index).

## Scope (builds the day agora + skill confirm the contract)

- Read `work:<id>` rows from the commons store (same version-cached loop
  as claims); map status → board columns; Backlog page gains the hub lane.
- Union precedence: hub work: rows (cross-agent truth) ∪ claim rows (who/
  liveness) ∪ gateway files (local deep view); one item never renders
  twice (id-keyed dedup).
- Backfill continuum's own 0011-0016 as the first rows.

## Receipts (completed 2026-07-20)

- Board reads work: rows (file-words-only shape, skill c3339 + c3343);
  columns derive per the S0 rule; gateway file cards win dedup.
- 0011-0016 backfilled as first rows; live-verified (0016 rendered In
  Progress from its own row + claim). Suite 302 green. Receipt c3344.
- agora ask 1 (validation/list) additive when it lands. Backlog-page hub
  lane = follow-up (board was the ruled priority view, dm 94).
