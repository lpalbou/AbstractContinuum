# Recurrent tasks

Periodic passes that keep the repo honest:

- `backlog_hygiene.md` — counts/ledgers/filenames/docs alignment after
  every wave; monthly fallback.
- `observer_boundary_audit.md` — the split's purpose boundary (observe vs
  develop) after any feature addition.
